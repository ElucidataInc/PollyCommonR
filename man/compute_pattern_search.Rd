% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compute_pattern_search.R
\name{compute_pattern_search}
\alias{compute_pattern_search}
\title{compute_pattern_search}
\usage{
compute_pattern_search(
  norm_gct_object,
  cohort_col,
  pattern = NULL,
  dist.name = "pearson"
)
}
\arguments{
\item{norm_gct_object}{the GCT object (metab_reactive$norm_gct_object)}

\item{cohort_col}{character(1) — which @cdesc column holds groups}

\item{pattern}{character(1) — e.g. "1-2-3"}

\item{dist.name}{character(1) — correlation method}
}
\value{
list(cor_mat, pattern, method, groups) or NULL on error
}
\description{
Shiny-facing wrapper called from observeEvent in the server above.
Reads @mat and @cdesc from norm_gct_object 
In normal server flow this is NOT called directly — the observeEvent above
handles the full flow including filter_metadata_by_cohorts and
sample_intensity_matrix before calling run_pattern_search_from_gct.
This function is provided for cases where you want to call the algorithm
directly without the full filter flow (e.g. scripted batch runs).
}
\keyword{internal}
