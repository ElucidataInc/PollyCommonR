% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compute_pattern_search.R
\name{run_pattern_search_from_gct}
\alias{run_pattern_search_from_gct}
\title{run_pattern_search_from_gct
norm_mat rows and metadata_df rows must already be aligned (same samples,
same order) — PollyCommonR::sample_intensity_matrix() in the server
guarantees this before this function is called.}
\usage{
run_pattern_search_from_gct(
  norm_mat,
  metadata_df,
  cohort_col,
  pattern = NULL,
  dist.name = "pearson"
)
}
\arguments{
\item{norm_mat}{matrix, samples x features (already transposed from @mat)}

\item{metadata_df}{data.frame, samples x metadata cols (filtered @cdesc)}

\item{cohort_col}{character(1) — which column holds group labels}

\item{pattern}{character(1) — e.g. "1-2-3", NULL auto-generates}

\item{dist.name}{character(1) — "pearson", "spearman", or "kendall"}
}
\value{
list(cor_mat, pattern, method, groups) or stops with message
}
\description{
run_pattern_search_from_gct
norm_mat rows and metadata_df rows must already be aligned (same samples,
same order) — PollyCommonR::sample_intensity_matrix() in the server
guarantees this before this function is called.
}
